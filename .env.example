# Базовый URL API без завершающего слэша (вид `/api/v1` или `https://api.example.com`)
# VITE_API_BASE_URL=/api/v1

# Прокси к Frankfurter v2 для курсов валют (dev: /exchange-rates через vite.config.ts)
# VITE_EXCHANGE_RATES_BASE_URL=/exchange-rates
